fastapi
uvicorn[standard]
python-multipart
httpx
pydantic
pydantic-settings
# OpenVINO GenAI — the default runtime.
openvino
openvino-genai
openvino-tokenizers
# ONNX Runtime GenAI — the second runtime, optional. The first package runs
# ONNX models on CPU; the second adds the OpenVINO execution provider that
# puts them on the NPU. It may pin a different OpenVINO version than
# openvino-genai wants, so it is opt-in rather than installed by default.
#   pip install onnxruntime-genai onnxruntime-openvino
mcp
tomli-w
apscheduler
# PyGObject: install via dnf (python3-gobject), not pip
sounddevice
numpy
openai-whisper
torch>=2.5.0
torchaudio>=2.5.0
transformers>=4.57.0,<5
soundfile>=0.12
safetensors>=0.4
tomli; python_version < "3.11"
huggingface_hub
Pillow
trafilatura>=1.12
ddgs>=9.0
playwright>=1.40
pytest
pytest-asyncio
