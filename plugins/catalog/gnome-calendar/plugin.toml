# Keylane catalog entry. Present in the repository but not installed
# until the user asks for it.
#
# Talks to the Fedora / GNOME Calendar stack through Evolution Data Server
# (the same backend GNOME Calendar uses). Requires python3-gobject and the
# ECal / EDataServer typelibs (gir1.2-ecal / evolution-data-server).
id = "gnome-calendar"
name = "GNOME Calendar"
kind = "native"
description = "List and create events in the Fedora built-in calendar via Evolution Data Server. Tools only — not a routing destination."
homepage = "https://apps.gnome.org/Calendar/"
worker_id = ""
cloud = false
tags = "tools, local, calendar"
entry = "plugin.py"
